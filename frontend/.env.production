# Production build — point the bundle at the deployed Render backend.
# Vite loads this file automatically during `npm run build` (mode=production).
VITE_API_BASE_URL=https://cardioinsight-backend-i0b3.onrender.com
